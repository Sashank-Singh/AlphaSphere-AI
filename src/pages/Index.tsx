
import React from 'react';

// This component is no longer used for routing
// It's kept as a placeholder and redirects to the home page
const Index: React.FC = () => {
  // Redirect is now handled by the router in App.tsx
  return null;
};

export default Index;
